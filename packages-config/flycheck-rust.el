;;; flycheck-rust --- Basic configuration for flycheck-rust

;;; Commentary:

;;; Code:

;; (add-hook 'rust-mode-hook
;; 
;;      '(lambda ()
;; 	(add-hook 'flycheck-mode-hook #'flycheck-rust-setup)))
;; 
;; (provide 'flycheck-rust)
;; 
;;; flycheck-rust ends here
